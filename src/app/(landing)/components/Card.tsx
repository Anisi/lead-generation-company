import Stack from '@/app/components/Stack';
import styled from 'styled-components';

const Card = styled(Stack)`
  padding: 24px;
  border-radius: 16px;
  background-color: #ffffff;
  box-shadow: 0 24px 34px 0 rgba(0, 0, 0, 0.04);
`;

export default Card;
